{
  "env": {
    "LANGFLOW_URL": "${LANGFLOW_URL}",
    "AUTH_TOKEN": "${AUTH_TOKEN}"
  },
  "build": {
    "env": {
      "LANGFLOW_URL": "${LANGFLOW_URL}",
      "AUTH_TOKEN": "${AUTH_TOKEN}"
    }
  }
} 